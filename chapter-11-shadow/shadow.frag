#version 330 core 


//float LinearizeDepth(float depth)
//{
//	float z = 2 * depth - 1;//back to NDC
//	return (2 * far * near) / (far + near - z * (far - near));
//}

void main()
{
	//float dep = LinearizeDepth(texture(shaderTex,texcoords).r);
	//color = vec4(vec3(dep)/ far .0,1.0);
}
